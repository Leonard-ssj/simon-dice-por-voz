# ============================================================
# requirements.txt — Servidor PC (Simon Dice por Voz)
#
# Instalar con:
#   pip install -r requirements.txt
#
# O usando el script de setup:
#   Windows: setup.bat
# ============================================================

# Reconocimiento de voz
openai-whisper      # Whisper local — descarga ~244MB la primera vez (modelo small)
numpy               # Procesamiento numerico de audio

# Audio pipeline
scipy               # Filtros DSP (HPF, LPF, resample)
noisereduce         # Sustraccion espectral del ruido ADC
sounddevice         # Captura de audio del microfono (PTT del simulador)

# Comunicacion
pyserial            # Puerto Serial con ESP32
websockets>=14      # Servidor WebSocket para el panel web
